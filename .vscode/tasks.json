{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build unit tests",
            "type": "shell",
            "command": "bazel build --config debug //lib/myers:test",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}